version: '3.5'

networks:
  mdb-network:
    name: mdb-network

services:
  mdb-api:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    platform: linux/x86_64
    ports:
      - "8455:8455"
    networks:
      - mdb-network
